# Base URL of the LifeSync backend API. Vite only exposes env vars prefixed
# with VITE_ to client-side code — this is intentional (see Vite docs), don't
# rename this without updating axiosInstance.js.
VITE_API_BASE_URL=http://localhost:8080/api
